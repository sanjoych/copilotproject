version: '3.8'

services:
  copilotproject:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: copilot-project
    restart: "no"
    command: ["127.0.0.1 80"]  # Default IP address